{
  "entities": {
    "Session": {
      "title": "Session",
      "description": "Stores the active session ID for each user to prevent multiple logins.",
      "type": "object",
      "properties": {
        "sessionId": {
          "type": "string",
          "description": "The unique ID of the current active session."
        },
        "lastUpdated": {
          "type": "string",
          "format": "date-time",
          "description": "When the session was last updated."
        },
        "username": {
          "type": "string",
          "description": "The username associated with this session."
        }
      },
      "required": ["sessionId", "lastUpdated", "username"]
    }
  },
  "firestore": {
    "sessions/{username}": {
      "schema": "Session",
      "description": "Collection of active sessions indexed by username."
    }
  }
}
